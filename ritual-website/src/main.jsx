import React from "react";
import ReactDOM from "react-dom/client";
import RitualDriftStore from "./RitualDriftStore.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RitualDriftStore />
  </React.StrictMode>
);
